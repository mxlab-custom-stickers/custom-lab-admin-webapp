{
  "name": "custom-lab",
  "private": true,
  "packageManager": "yarn@4.7.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:configurator": "yarn workspace @clab/configurator build",
    "build:firebase": "yarn workspace @clab/firebase build",
    "build:types": "yarn workspace @clab/types build",
    "build:utils": "yarn workspace @clab/utils build"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "prettier-plugin-organize-imports": "^4.1.0",
    "prettier-plugin-tailwindcss": "^0.6.12",
    "typescript": "^5.8.3"
  }
}
